<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.practice04.dao.Realtor2DAO">
 	<insert id="insertRealtor" parameterType="com.quiz.practice04.model.Realtor2" 
 	useGeneratedKeys="true" keyProperty="id">
 	INSERT INTO `realtor`
		(
			`office`
			, `phoneNumber`
			, `address`
			, `grade`
			, `createdAt`
			, `updatedAt`
		)
		VALUES
		(
			#{office}
			, #{phoneNumber}
			, #{address}
			, #{grade}
			, NOW()
			, NOW()
		)
 	
 	</insert>
 
 	<select id="selectRealtor" parameterType="int" resultType="com.quiz.practice04.model.Realtor2">
 	SELECT
			`id`
			, `office`
			, `phoneNumber`
			, `address`
			, `grade`
			, `createdAt`
			, `updatedAt`
		FROM
			`realtor`
		WHERE
			`id` = #{id}
 	
 	</select>
 </mapper>